#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;

    int cur = 0, mx = 0, a, b;

    while (n--) {
        cin >> a >> b;
        cur -= a;
        cur += b;
        mx = max(mx, cur);
    }

    cout << mx;
    return 0;
}
