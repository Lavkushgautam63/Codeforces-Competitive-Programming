#include <iostream>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    int current = 1;
    while (m >= current) {
        m -= current;
        current++;
        if (current > n) current = 1;
    }
    cout << m << endl;
}
